# TDX measurement verification tool

[verify.quex.tech](https://verify.quex.tech/)

## Launching locally

Serve the files in the `docs/` directory with any tool, for example:

```sh
python -m http.server -d docs/
```
